node_modules
dist
dist-test
dist-sandbox
*.log
*.duckdb
*.duckdb.wal

.DS_Store

# Local AI agent overlays (skills, settings). Mirrors qyl/.gitignore so an
# identical overlay file behaves the same in both repos.
.claude/settings.local.json
.claude/settings.local.json.*
