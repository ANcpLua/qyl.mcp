node_modules
dist
dist-test
dist-sandbox
*.log
*.duckdb
*.duckdb.wal

.DS_Store

# Machine-local Claude Code settings. Mirrors qyl/.gitignore so an identical
# settings file behaves the same in both repos. Skills under .claude/ are
# tracked.
.claude/settings.local.json
.claude/settings.local.json.*
