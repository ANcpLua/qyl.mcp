/** Dashboard aliases over the published runner contract. */
export type {
  RunnerLogLine as LogLine,
  RunnerResourceLifecycle as ResourceLifecycle,
  RunnerResourceState as ResourceState,
} from "@ancplua/qyl-api-schema/types";
