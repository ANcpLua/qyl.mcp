{
  "name": "qyl.mcp",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "description": "qyl MCP host, telemetry tools, MCP Apps viewers, and local resource dashboard",
  "workspaces": [
    "server",
    "runner",
    "dashboard"
  ],
  "scripts": {
    "build": "npm run build --workspace server && npm run build --workspace runner && npm run build --workspace dashboard",
    "test": "npm run test --workspace server && npm run test --workspace runner && npm run test --workspace dashboard",
    "start": "node server/dist/main.js",
    "start:runner": "node runner/dist/main.js",
    "smoke": "npm run smoke --workspace server && npm run smoke --workspace runner",
    "smoke:otlp": "npm run smoke:otlp --workspace runner",
    "dev": "npm run build --workspace server && npm run dev --workspace dashboard"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "qyl-mcp-dashboard": "^0.1.1",
    "qyl-mcp-runner": "^0.1.1",
    "qyl-mcp-server": "^0.1.1",
    "vite": "8.1.5"
  }
}
